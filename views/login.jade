extends layout
block head
    script(src='/socket.io/socket.io.js')
    script
        // Testing SocketIO so that we can see what happens when user hasn't authenticated
        console.log('login.jade : Loaded.');
        var socket = io.connect();
block content
    form(action='/login', method='post', id='loginForm')
        div
            label Username:
            input(type='text', name='username')
            br
        div
            label Password:
            input(type='password', name='password')
        div
            input(type='submit', value='submit')
